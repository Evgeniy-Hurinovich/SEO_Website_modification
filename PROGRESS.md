# A2 Consulting SEO — статус работ

**Сайт:** https://a2c.by/  
**Платформа:** 1С-Битрикс 25.750 · Aspro Allcorp3  
**Обновлено:** 3 августа 2026 (вечер)

---

## Фаза A — Индексация и обход (DONE)

| Пункт | Статус | Результат |
|-------|--------|-----------|
| **A1** robots.txt | ✅ | Заполнен стартовым набором Bitrix; публично https://a2c.by/robots.txt не пустой; есть `Sitemap: https://a2c.by/sitemap.xml` |
| **A2** sitemap.xml | ✅ | Сгенерирован 03.08.2026 ~15:14; https://a2c.by/sitemap.xml → **200** (sitemapindex, 8 дочерних карт). Настройка ID=1 «Карта a2c.by», сайт **s1**, **https**, домен **a2c.by** |
| **A3** GSC / Вебмастер | ⏳ | Ссылки выданы; отправку sitemap подтвердить у команды (если ещё не отправили) |
| **A4** canonical | ✅ | Услуги / проекты / новости: `DETAIL_SET_CANONICAL_URL=Y` в `index.php`; шаблоны `CANONICAL_PAGE_URL` в инфоблоках 42/41/38. Детальные URL отдают корректный `rel=canonical` (новости починены — убраны лишние пробелы в поле) |

### Важные пути админки (фаза A)

- robots: `/bitrix/admin/seo_robots.php?lang=ru`
- sitemap: `/bitrix/admin/seo_sitemap.php?lang=ru` (редакт. ID=1)
- кеш: `/bitrix/admin/cache.php?lang=ru` → «Очистка файлов кеша» → Все → Начать
- canonical в инфоблоке: вкладка **«Инфоблок»** (не «Поля»), поле «Канонический URL элемента»
- компоненты:  
  - `/services/index.php`  
  - `/projects/index.php`  
  - `/news/index.php`  
  параметр `"DETAIL_SET_CANONICAL_URL" => "Y"`

### Sitemap: что включено в инфоблоках

**Включены (левая галочка):** 19, 20, 25–30, 34–43, 46 (в т.ч. услуги 42, проекты 41, новости 38, статьи 37, акции 39, обзоры 46).

**Выключены:** 1–18, 21–24, 31–33, 44, 45, 47, 48 (формы, баннеры, UI, ЛК).

В индексе после генерации были дочерние: files, iblock 25/27/36/38/40/41/42 (пустые iblock-файлы могут не создаваться).

### Canonical — проверено снаружи

- Детали услуг/проектов: OK (`https://a2c.by/...` без www).
- Новости: после удаления ~20 пробелов в шаблоне — OK (4/4).
- Главная, списки разделов: без canonical — ожидаемо (галка только на детальный просмотр).

Шаблоны canonical:

```
Услуги 42: https://a2c.by#SITE_DIR#services/#SECTION_CODE_PATH#/#ELEMENT_CODE#/
Проекты 41: https://a2c.by#SITE_DIR#projects/#SECTION_CODE_PATH#/#ELEMENT_CODE#/
Новости 38: https://#SERVER_NAME##SITE_DIR#news/#ELEMENT_CODE#/
```

---

## Не из фазы A (зафиксировано)

- Уведомление «Проверка системы» / `site_checker.php` — **не из‑за robots/sitemap**.
- Красное: **сокеты (loopback)** — к хостингу; из‑за этого многие доп. проверки «не удалось».
- Замечание: агенты на хитах → лучше cron.
- Ошибка пути сайта: «путь к корню должен быть пустой» (`/var/www/h97995/data/www/a2c.by`) — хостинг/настройки сайта.
- `www.a2c.by` и `a2c.by` оба отдают 200 (дубль зеркал) — частично закрыто canonical на `a2c.by`; полный редирект www→non-www ещё желателен позже.

---

## Завтра: фаза B — Скорость как SEO-сигнал

**Старт со спринта 1:** B1–B3 (и параллельно помнить C1).

| Пункт | Что | Где |
|-------|-----|-----|
| **B1** | Включить АвтоКомпозит | Настройки → Композитный сайт → АвтоКомпозит |
| **B2** | JS в конец + сжатые копии CSS/JS | Настройки main → Оптимизация CSS/JS |
| **B3** | Hero баннеров → WebP ≤120 KB | Реклама → Большие баннеры (IB 23) |
| B4 | Отложить Метрику и CRM-формы | позже |
| B5 | Perfmon / хостинг | позже (FS/Perfmon уже плохие в аудите) |

**Контекст аудита:** Performance 47, LCP 6.4 с, render-blocking ~2.57 с, АвтоКомпозит был **выкл.**, Perfmon 11.4.

**Отчёт:** https://github.com/Evgeniy-Hurinovich/SEO_Website_modification — вкладка «План видимости» (`#visibility-plan`), чекбоксы в localStorage браузера.

**Правило:** живой a2c.by не менять без явного подтверждения шага; вести по инструкции в админке, как A1–A4.

---

## Репозиторий / отчёт

- Код отчёта: `index.html` (+ зеркало `anna-audit-report/index.html`)
- Секреты: только локальный `.env` (не в git)
- Вспомогательные `_check_*.py` / `_a4_*.py` — локальные скрипты проверки, в git не обязательны
